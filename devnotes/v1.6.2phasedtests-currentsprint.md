v1.6.2phasedtests-currentsprint.md
❯ please give me explicit instructions for testing each of these phases. this is ALOT!

● You're right, it's a lot. Let me give you a clear, ordered testing checklist. Each phase builds on the previous,
  so go in order.

  ---
  Phase 1: Instagram Feed Image Generators

  Test the new routes visually:
  *PASSED* 1. Visit https://tarottalks.app/cards/the-tower/instagram-feed - should return a 1080x1350 PNG
  *PASSED* 2. Visit https://tarottalks.app/talks/brene-brown-the-power-of-vulnerability/instagram-feed - same format,
  talk-forward layout
  *PASSED* 3. Compare with existing square versions:
    - https://tarottalks.app/cards/the-tower/instagram (1080x1080 for reference)

  Test the storage pipeline:
  *PASSED* 4. POST to https://tarottalks.app/api/admin/share-images/generate with this body:
  {
    "category": "cards",
    "slug": "the-tower",
    "types": ["instagram-feed"]
  }
  *PASSED* 5. Check response has { "results": [{ "type": "instagram-feed", "success": true, "url": "..." }] }
  *PASSED* 6. Verify the image exists in Supabase Storage under share-images/cards/instagram-feed/the-tower.png

  Test LinkedIn type (reuses OG generator, stored under linkedin path):
  *PASSED* 7. POST same endpoint:
  {
    "category": "cards",
    "slug": "the-tower",
    "types": ["linkedin"]
  }
  *PASSED with notes* 8. Verify share-images/cards/linkedin/the-tower.png appears in Supabase
*notes: CORRECTED PATH: share-images/cards/opengraph/the-tower.png*
  ---
  Phase 2: Campaign CLI Pipeline

  You need to deploy first since the scripts hit the DB via dotenv -e .env.local.

  Test ingest:
  npm run campaign:ingest -- --source signal-deck --campaign test-01 --platform bluesky --limit 3
*PASSED* - Should see emoji logs: Found X items, Y new, manifest written
*PASSED*- Check campaigns/test-01/manifest.json exists with items array
*PASSED* - Check directory skeleton: campaigns/test-01/bluesky/ with date-slug folders

  Test download:
  npm run campaign:download -- --campaign test-01
*PASSED with notes*  - Should download source.png into each post directory 
*notes: the files have generic names "source.png"... this could be problematic in the future.*
*PASSED*  - Run again to verify idempotency (should say "Skipped: 3")

  Test render:
  npm run campaign:render -- --campaign test-01
*PASSED*  - Check each post directory for og_card.jpg (or whatever specs match bluesky)
*PASSED*  - Check caption.txt and metadata.json exist

  Test index:
  npm run campaign:index -- --campaign test-01
*PASSED*  - Check `campaigns/test-01/bluesky/_index.csv` - should have rows
*PASSED*  - Check `campaigns/test-01/_summary.json` - platform totals

*PASSED with notes*  Test JSON source (for Phase 5 export integration):
*notes: i dont see where this file is saved... should I? for now will continue through Phase 5 as it might become clear*
  Create a file test-input.json:
  {
    "items": [
      {
        "platform": "instagram",
        "slug": "the-tower",
        "category": "cards",
        "image_url": "",
        "caption": "Test caption",
        "alt_text": "The Tower tarot card",
        "tags": ["tarot", "tower"]
      }
    ]
  }
  npm run campaign:ingest -- --source json --file test-input.json --campaign test-02

  ---
  Phase 3: UGC Styling

  Test via the render script with --ugc flag:
  npm run campaign:render -- --campaign test-01 --ugc
*PASSED*  - Check post directories for ugc_og_card.jpg alongside the clean og_card.jpg
*PASSED*  - Visually compare the two: UGC version should have subtle grain, slightly warmer colors, and a faint
  "tarottalks.app" watermark bottom-right
*PASSED*  - File size should be slightly smaller (quality 88 vs 90)

  ---
  Phase 4: Gemini Caption Generation

  Test via render with --captions flag:
  npm run campaign:render -- --campaign test-01 --captions
*CURRENT PROGRESS...*
*FAILED with output*  - Check each post directory for: caption_short.txt, caption_medium.txt, caption_spicy.txt, hashtags.txt
*output:*
```
bam@BTU-LUFFY:~/projects/TarotTALKS$ npm run campaign:render -- --campaign test-01 --captions

> tarottalks@1.6.1 campaign:render
> dotenv -e .env.local -- tsx scripts/campaign-render.ts --campaign test-01 --captions

🚀  Rendering campaign: test-01
📋  3 items to render
[dotenv@17.2.3] injecting env (0) from .env.local -- tip: ✅ audit secrets and track compliance: https://dotenvx.com/ops
✍️  Caption generation enabled
📊  Rendered: 0, Skipped: 3, Errors: 0
✅  Render complete!
bam@BTU-LUFFY:~/projects/TarotTALKS$
```
  - Open each and verify they're platform-appropriate (bluesky = conversational, no hashtags in text)
  - Watch the console for Gemini cost logs (should be ~$0.001 per item)
  - Note: This calls the live Gemini API and counts against your budget

  ---
  Phase 5: Signal Deck UI

  All tested in browser at the admin Signal Deck page.

  Hashtag Suggester:
  1. Open Signal Deck admin
  2. Click "New Share"
  3. Select Instagram as platform
  4. Enter a TarotTALKS card URL (e.g., https://tarottalks.app/cards/the-tower)
  5. Below the Notes field, you should see "Hashtag Suggestions" with clickable tags
  6. Click several tags, then click "Copy X" - paste somewhere to verify

  Post Preview:
  7. In the same form, type something in Notes
  8. Click "Show Preview" below the notes area
  9. Should see an Instagram-style mockup with your caption text
  10. Switch platform to X - preview should change to tweet-style
  11. Switch to LinkedIn - should show article card style

  Single Share Export:
  12. Save or cancel the form
  13. On any existing share row, find the new download icon (between rescan and edit)
  14. Click it - should download a .json file
  15. Open the JSON - should have an items array with that share's data

  Multi-Select + Bulk Export:
  16. Click the "Select" button in the header (next to "New Share")
  17. Checkboxes appear on each share row
  18. Check 2-3 shares
  19. Click "Export 3" green button that appears
  20. Modal shows selected shares, click "Download JSON"
  21. Verify the downloaded JSON has all selected items
  22. Click "Cancel" to exit select mode

  ---
  Phase 6: All-in-One UGC Script

  The big integration test:
  npm run campaign:ugc -- --campaign ugc-test --platform instagram --limit 3
  - Should run all 4 steps automatically with progress logs
  - Verify campaigns/ugc-test/ has:
    - manifest.json
    - _summary.json
    - instagram/_index.csv
    - Post directories with: source.png, feed_4x5.jpg, square_1x1.jpg, ugc_feed_4x5.jpg, ugc_square_1x1.jpg,
  caption_short.txt, caption_medium.txt, caption_spicy.txt, hashtags.txt, metadata.json

  Run it again to verify idempotency - should skip everything and finish fast.

  ---
  Quick Smoke Test Order

  If you want to test the minimum path quickly:

  1. Deploy
  2. Visit one instagram-feed URL in browser (Phase 1)
  3. Run campaign:ugc -- --campaign smoke --platform bluesky --limit 2 (Phases 2-4 + 6)
  4. Open Signal Deck admin, create an Instagram share, check hashtags + preview (Phase 5)

  That covers the critical path in ~5 minutes.