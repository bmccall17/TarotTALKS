# v1.6.3 - Vercel Free-Tier Optimization: ISR & CDN Caching

**Released:** Feb 7, 2026

## Summary
Comprehensive caching pass to reduce Vercel paid usage (Function Execution, Source Image, Fast Origin Transfer) that spiked around Jan 29th. Added 24-hour ISR caching to all dynamic OG/Twitter image routes, switched detail pages from SSR to ISR, and enabled CDN caching on the search API. Partial ship — listing page caching deferred due to Vercel build timeout constraint.

---

## Features
- **OG/Twitter image caching (Phase 1):** All 5 uncached dynamic image routes now have `revalidate = 86400` (24h ISR). Social bot crawls and link previews no longer trigger serverless function + Satori PNG generation on every request.
- **Detail page ISR (Phase 2):** Card, talk, and theme detail pages switched from `revalidate = 0` (SSR every request) to ISR caching. Cards/themes cache for 24h, talks cache for 1h. `generateStaticParams()` returns empty array to skip build-time pre-rendering.
- **Search API CDN caching (Phase 3):** Search results now cached at CDN for 60 seconds with 5-minute stale-while-revalidate, reducing function execution for repeated queries.
- **Test script:** Added `scripts/test-vercel-optimization.sh` to verify all caching configurations across all 4 phases + guard rails for Instagram routes.
- **Audit workflow (`/audit`):** New agent workflow for repeatable codebase audits across 8 dimensions (API wiring, Gemini model usage, architecture, security, hygiene, cost, resilience, database) with 71 concrete checks. Produces versioned reports at `devnotes/audits/YYYY-MM-DD.md`. Supports single-dimension (`/audit security`), multi-dimension, and `--skip` invocation.

## Known Workaround
- **Listing pages (`/cards`, `/talks`, `/themes`) remain SSR (`revalidate = 0`).** ISR causes Vercel build timeout (Supabase queries exceed 60s during build). Documented in `devnotes/vercel-optimizations-feb-2026.md` with future fix options.

---

## Files Changed

### New Files
- `scripts/test-vercel-optimization.sh` - Automated test suite for all caching configurations (17 checks)
- `.agent/workflows/audit.md` - `/audit` workflow: 8-dimension codebase audit with 71 checks (API wiring, Gemini, architecture, security, hygiene, cost, resilience, database)
- `devnotes/audits/.gitkeep` - Output directory for versioned audit reports

### Modified Files
- `app/cards/[slug]/opengraph-image.tsx` - Added `revalidate = 86400`
- `app/cards/[slug]/twitter-image.tsx` - Added `revalidate = 86400`
- `app/talks/[slug]/twitter-image.tsx` - Added `revalidate = 86400`
- `app/spreads/[id]/opengraph-image.tsx` - Added `revalidate = 86400`
- `app/spreads/[id]/twitter-image.tsx` - Added `revalidate = 86400`
- `app/cards/[slug]/page.tsx` - `revalidate = 0` to `86400`, emptied `generateStaticParams()`, removed unused `getAllCards` import
- `app/talks/[slug]/page.tsx` - `revalidate = 0` to `3600`, emptied `generateStaticParams()`, removed unused `getAllTalks` import
- `app/themes/[slug]/page.tsx` - `revalidate = 0` to `86400`, emptied `generateStaticParams()`, removed unused `getAllThemes` import
- `app/api/search/route.ts` - Cache-Control header: `no-store, max-age=0` to `public, s-maxage=60, stale-while-revalidate=300`
- `devnotes/vercel-optimizations-feb-2026.md` - Full implementation log + workaround documentation

---

## Testing Checklist
- [ ] Build deploys successfully on Vercel (no timeout errors)
- [ ] `bash scripts/test-vercel-optimization.sh` passes 17/17
- [ ] Share a card link on https://www.xcardvalidator.com/ — second hit within 24h should be instant
- [ ] Share a talk link on https://www.xcardvalidator.com/ — same behavior
- [ ] Visit https://tarottalks.app/cards/the-tower — check Vercel logs for ISR `HIT` on repeat visits
- [ ] Visit https://tarottalks.app/talks — confirm page loads (still SSR)
- [ ] Run same search twice — second should be faster (CDN cache hit)
- [ ] Monitor Vercel dashboard for 48h to confirm usage trends down
- [ ] Instagram routes still generate fresh images (not cached)
