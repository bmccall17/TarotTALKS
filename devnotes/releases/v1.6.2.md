# v1.6.2 - Social Campaign Asset Pipeline & Signal Deck Parity 📸🎨

**Released:** Feb 6, 2026

## Summary
Multi-platform social campaign CLI pipeline with Instagram image parity, UGC styling, AI caption generation, and Signal Deck UI enhancements for export, previews, and hashtag suggestions.

**Sprint Status:** Phases 1-4 tested and passing. Phases 5-6 code complete, testing in progress.

---

## Features

### Phase 1: Instagram Feed Image Generators
- Instagram Feed portrait (1080x1350) image routes for cards and talks
- Extracted shared Satori helpers (fonts, sparkles) into `satori-helpers.ts`
- Added `instagram-feed` and `linkedin` types to storage pipeline
- LinkedIn images stored via OG generator under `linkedin` bucket path

### Phase 2: Campaign CLI Pipeline
- `campaign:ingest` - Build manifest from Signal Deck DB, CSV, or JSON input
- `campaign:download` - Fetch source images with 3x retry + exponential backoff
- `campaign:render` - Sharp resize to platform specs (Instagram 4:5 + 1:1, X/Bluesky/LinkedIn OG cards)
- `campaign:index` - Generate per-platform `_index.csv` and `_summary.json`
- All scripts idempotent (safe to re-run), follow existing `dotenv + tsx` pattern
- Output to `/campaigns/` directory (gitignored)

### Phase 3: UGC Image Styling
- Sharp-based treatments: slight crop offset, grain noise overlay, warm color shift, SVG watermark, JPEG quality 88
- Produces `ugc_` prefixed variants alongside clean renders via `--ugc` flag

### Phase 4: Gemini Caption Generation
- Platform-aware AI captions (short/medium/spicy variants + hashtags)
- Reuses existing `generateWithGemini()` with retry, rate limiting, cost tracking
- ~$0.0001 per item via Gemini 2.0 Flash
- Integrated into render script via `--captions` flag

### Phase 5: Signal Deck UI Enhancements
- **PostPreview** component - Platform-specific post mockups (IG, X, Bluesky, LinkedIn)
- **HashtagSuggester** component - Card-aware hashtag pools with click-to-toggle and copy
- **ExportToCampaign** component - Bulk export selected shares as campaign JSON
- Multi-select mode in SharesList with "Export Selected" bulk action
- Per-share export button (download icon) on each ShareRow

### Phase 6: All-in-One UGC Script
- `campaign:ugc` - Single command: ingest -> download -> render (UGC + captions) -> index

---

## Files Changed

### New Files (15)
- `lib/utils/satori-helpers.ts` - Shared font loader, sparkle generator, font options builder
- `app/cards/[slug]/instagram-feed/route.tsx` - 1080x1350 card image generator
- `app/talks/[slug]/instagram-feed/route.tsx` - 1080x1350 talk image generator
- `lib/campaign/types.ts` - CampaignItem, CampaignManifest, PLATFORM_RENDER_SPECS
- `lib/campaign/utils.ts` - CSV/JSON parsers, manifest I/O, path helpers
- `lib/campaign/ugc-style.ts` - Sharp UGC treatments (grain, warmth, watermark, crop)
- `lib/campaign/caption-generator.ts` - Gemini caption variants + hashtag generation
- `scripts/campaign-ingest.ts` - CLI: ingest from Signal Deck/CSV/JSON
- `scripts/campaign-download.ts` - CLI: download source images with retry
- `scripts/campaign-render.ts` - CLI: resize + UGC + captions
- `scripts/campaign-index.ts` - CLI: generate platform indexes
- `scripts/campaign-ugc.ts` - CLI: all-in-one pipeline
- `components/admin/signal-deck/PostPreview.tsx` - Platform post mockups
- `components/admin/signal-deck/HashtagSuggester.tsx` - Hashtag suggestion UI
- `components/admin/signal-deck/ExportToCampaign.tsx` - Bulk export modal

### Modified Files (7)
- `lib/supabase/share-image-storage.ts` - Added `instagram-feed`, `linkedin` to ImageType
- `app/api/admin/share-images/generate/route.ts` - Added new types + route mappings
- `package.json` - Version bump, 5 campaign script aliases
- `.gitignore` - Added `/campaigns/`
- `components/admin/signal-deck/NewShareForm.tsx` - HashtagSuggester (IG/Threads), Preview toggle
- `components/admin/signal-deck/ShareRow.tsx` - Export button, multi-select checkbox
- `components/admin/signal-deck/SharesList.tsx` - Select mode, bulk export action

---

## Bug Fixes During Testing
- Fixed UGC variant generation skipped when clean render already existed (idempotency bug)
- Fixed caption generation skipped when clean render already existed (same class of bug)
- Added missing log confirmation for caption file writes

---

## Testing Checklist
- [x] Phase 1: Instagram-feed routes render 1080x1350 images
- [x] Phase 1: Storage pipeline accepts instagram-feed and linkedin types
- [x] Phase 2: campaign:ingest from Signal Deck creates manifest + directories
- [x] Phase 2: campaign:download fetches source images with idempotency
- [x] Phase 2: campaign:render produces platform-spec images
- [x] Phase 2: campaign:index generates CSV indexes and summary
- [x] Phase 2: campaign:ingest from JSON source works
- [x] Phase 3: --ugc flag produces grain/warmth/watermark variants
- [x] Phase 4: --captions flag generates AI caption variants via Gemini
- [ ] Phase 5: HashtagSuggester visible for Instagram shares
- [ ] Phase 5: PostPreview renders platform mockups
- [ ] Phase 5: Single share export downloads JSON
- [ ] Phase 5: Multi-select + bulk export works
- [ ] Phase 6: campaign:ugc all-in-one pipeline end-to-end
- [ ] Phase 6: Idempotency on re-run
- [ ] All tests on https://tarottalks.app
